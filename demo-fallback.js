/* Plan B for the live demo (slide 22): captured output of the exact demo prompt on the starter repo, read-only. Press B on slide 22. */
window.DEMO_FALLBACK = "Claude Code · model: Sonnet · repo: aetherlink-classroom-starter @ 373de89 · captured 18 Sep 2026 · read-only\n\n> Explore this repository without changing anything. …\n\nI explored the full repo (no files changed). Here's what it is and how to verify each claim.\n\n## What it does\n\n**Aether Library** is a small classroom starter app for a course (\"AetherLink Worldline Wave 2\"). It's a single-page app with four sections (Profiles, Glossary, Library, Game) served by a zero-dependency Node server. Only the **Game** (\"Explain It Back\") is functional; Profiles, Glossary, and Library are empty placeholders participants build out during the course.\n\nThe Game: shows a random glossary term, the user writes an explanation, submits it, then asks their own Claude Code terminal session to grade it against `checklist.md` and write feedback to a file — no network/API call involved, the browser and Claude Code communicate only via shared JSON files on disk.\n\n## Structure — with evidence\n\n**Server** (`server.js`, 162 lines, uses only Node's built-in `http`/`fs`, no framework):\n- `GET /api/glossary`, `GET /api/concept-cards` — stream `data/glossary.json` / `data/concept-cards.json` as-is (`server.js:143-148`)\n- `POST /api/submissions` — validates `term`+`explanation` are non-empty strings, writes `data/latest-submission.json`, and deletes any stale `data/latest-feedback.json` (`server.js:61-90`)\n- `GET /api/feedback` — reads `data/latest-feedback.json` if present, returns `{available:false}` (200, not an error) if absent (`server.js:95-105`)\n- All other paths serve static files from `public/`, falling back to `index.html` for unknown paths — SPA-style routing (`server.js:107-138`)\n- No `/api/profiles` route exists — confirmed, no match for \"profiles\" anywhere in `server.js`.\n\n**Frontend** (`public/index.html` + `public/app.js`, vanilla JS, no build step, no framework):\n- Nav has all 4 buttons (`index.html:18-21`), but only `#view-game` has real markup/behavior; `#view-profiles`, `#view-glossary`, `#view-library` are empty `<div>` containers with no rendering code anywhere in `app.js`.\n- `initGame()` (`app.js:240-246`) wires Submit/Check feedback/Reveal/New term buttons and fetches `/api/glossary` + `/api/concept-cards` on load.\n- `revealConceptCard()` (`app.js:220-238`) looks up a concept card for the current term; if none exists, falls back to showing the raw glossary definition — matches the \"graduated\" data-model relationship described in `SPEC.md`.\n\n**Data** (`data/`):\n- `glossary.json` and `concept-cards.json` are both currently `[]` (verified by `cat`) — matches README/SPEC claims that they start empty.\n- `data/latest-submission.json` / `data/latest-feedback.json` don't exist on disk yet — confirmed via `ls` (no such file). They're also git-ignored (`.gitignore` lines 8-10), so a fresh clone or a completed round never shows up in git status.\n- `data/fixtures/glossary-complete.json` (2 valid entries) and `glossary-incomplete.json` (1 valid entry, 1 with an empty `definition`) exist, used only by `npm run validate:fixtures`.\n- `data/profiles.json` does not exist — confirmed, no such file in the repo listing.\n\n**Validation** (`scripts/validate.js`): checks required string fields are non-empty and required arrays are non-empty, per schema (`profile`/`glossary`/`concept-card`, defined at lines 43-54). Treats an empty top-level array as a valid pass (`validateFile`, lines 116-126). Auto-detects `data/profiles.json` via `access()` and only validates it if present (lines 185-188) — I did not execute this script, so I can't confirm it runs cleanly; that's **OPEN**, verify with `npm run validate`.\n\n**`checklist.md`**: literally just a title and one placeholder sentence — confirmed by reading the file directly, matches README's description.\n\n**Skills**: `.claude/skills/` does not exist in this repo — confirmed via the `find` listing (no `.claude` directory present at all). `create-concept-card` and `term-checker` are described in README/SPEC as course work, not present here.\n\n**Dependencies**: `package.json` has `\"dependencies\": {}` — confirmed, zero npm packages, so `npm install` needs no network. I did not run `npm install` or `npm start` myself — **OPEN**: whether the server actually starts and serves correctly is unverified by me; you can check with `npm start` and opening `http://localhost:3000`.\n\n**Git state**: one commit (`373de89`), working tree clean, matches the system-provided git status.\n\n## Summary of OPEN items (not directly verified by me)\n- Whether `npm start` actually boots a working server and the pages render correctly in a browser.\n- Whether `npm run validate` / `npm run validate:fixtures` execute without error.\n- Whether the SPA fallback and 403 path-traversal guard in `sendStatic` (`server.js:107-138`) behave correctly at runtime — I read the code but didn't exercise it with requests.";

/* Plan B for Assignment 10 (slide 70): captured run on a fictional fixture, read-only. Press B on slide 70. */
window.DEMO_FALLBACK_A10 = "Claude Code · model: Sonnet · fixture: fixtures/jira-EX-142.json (FICTIONAL) · starter branch cons/cursus-aanpassingen · captured 18 Sep 2026 · read-only\n\n> Retrieve one authorised item — fixtures/jira-EX-142.json, read-only. Explain … unclear … actions not approved … do not modify.\n\n## What was retrieved\n\nRead-only, no writes: `fixtures/jira-EX-142.json`, a fictional classroom fixture (its `_note` field says so explicitly — not a real Jira ticket).\n\n**Item:** EX-142, \"Show a clear message when a CSV export is empty\" — project \"Example Portal\", type Story, status **In Progress**, priority Medium.\n\n**Supporting fields:**\n- `description` — explains the bug: exporting an empty date range downloads a header-only CSV, which reads as a failure to users; the fix is to show an in-app message instead of a file.\n- `acceptanceCriteria` (3 items) — no download on empty range, exact message text `'No transactions in this period.'`, and non-empty exports must behave unchanged.\n- `labels`: `export`, `ux`.\n- `assignee: null`, `reporter: \"product-owner-example\"`.\n- `created` 2026-09-02, `updated` 2026-09-15.\n- `comments`: one entry from `dev-example` (2026-09-10) asking whether the message should also apply to the scheduled nightly export, or only manual ones.\n\n## What remains unclear\n\n- The open question in the comment (manual-only vs. also nightly export) has no recorded answer in this fixture — so scope for the scheduled-export case is undetermined.\n- No linked PR, branch, or commit is present in the file, so I can't tell how far implementation has actually progressed despite the \"In Progress\" status.\n- `assignee: null` — no owner is recorded.\n\n## Actions a real Jira connection could perform that I did not take\n\nI only read a local file — no Jira connection exists in this session. A real Jira MCP/API integration could additionally:\n- Change ticket status/workflow transitions (e.g., move to \"Done\", \"In Review\")\n- Edit summary, description, or acceptance criteria\n- Assign/reassign the ticket to a user\n- Add comments or reply to the open question\n- Add/remove labels, change priority\n- Link/unlink related issues or PRs\n- Attach files\n- Delete or archive the issue\n- Trigger webhooks/automations tied to the ticket\n\nNone of these were performed or requested — I did not approve or execute any of them.";
